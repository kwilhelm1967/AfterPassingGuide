# Copy to .env and fill in. Used by Vite (app) only; Supabase secrets are set in Dashboard.

# --- Supabase (required for admin portal and for Supabase-backed licensing) ---
# Project URL from Supabase Dashboard → Settings → API
VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co

# Optional: use Supabase Edge Functions for activation/transfer (if set, app uses these instead of default server)
VITE_APG_ACTIVATE_URL=https://YOUR_PROJECT_REF.supabase.co/functions/v1/apg-activate
VITE_APG_TRANSFER_URL=https://YOUR_PROJECT_REF.supabase.co/functions/v1/apg-transfer

# --- Optional: default license server (used only when VITE_APG_ACTIVATE_URL / VITE_APG_TRANSFER_URL are not set) ---
# VITE_LICENSE_SERVER_URL=https://server.example.com
